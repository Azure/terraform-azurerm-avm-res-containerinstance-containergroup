# ACR Managed Identity Example

This example demonstrates how to deploy an Azure Container Instance that pulls images from Azure Container Registry (ACR) using a user-assigned managed identity for authentication, without requiring username and password credentials.
